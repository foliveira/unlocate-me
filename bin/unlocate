#!/usr/bin/env node

'use strict'

var program = require('commander')
var metadata = require('../package')

program
  .version(metadata.version)
  .command('register [service]', 'register with a service')
  .command('update [service]', 'update your info with a service')

program.parse(process.argv)
